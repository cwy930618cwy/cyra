# 2026-09-02

## LyraPawnData 系列（新建文件夹）
- `LyraPawnData/` 文件夹，已写 3 篇：
  - `01_LyraPawnData概述_数据资产是什么.md` — DataAsset 概念、U vs A 区别、与 ALyraPawn 关系（配方 vs 成品）、数据驱动思想。配了联系图。
  - `02_五大配置字段详解.md` — PawnClass/AbilitySets/TagRelationshipMapping/InputConfig/DefaultCameraMode 逐个讲（身体/技能/规则/操作/视角）。UPROPERTY 都用 EditDefaultsOnly+BlueprintReadOnly（只配一次运行时只读）。
  - `03_LyraPawnData.cpp详解_构造函数在干嘛.md` — 只有一个构造函数：FObjectInitializer参数+必调Super+清零三个指针（其余有默认构造/系统自动清，非遗漏）。策划值靠序列化 .uasset 覆盖，不在构造函数。DataAsset 的 cpp 天生极简。
- 关键理解点（用户顿悟）：一个 C++ 类(一份) → 可创建多份 .uasset 资产实例 → 运行时 GameMode::GetPawnDataForController 判断读哪份。类比"表格/数据库行"：类=表头，每份资产=一行，代码=查表读行。

## LyraPawnExtensionComponent 系列（新建文件夹）
- `LyraPawnExtensionComponent/` 文件夹，已写 1 篇：
  - `01_LyraPawnExtensionComponent概述_初始化总指挥.md`
- 核心：Pawn 的"初始化总指挥/大管家"，不自己干活，专协调其他组件(ASC/输入/镜头)在正确时机初始化。
- 关键机制=初始化状态链：Spawned→DataAvailable→DataInitialized→GameplayReady（齐步走，由 UGameFrameworkComponentManager 管）。
- 持有 PawnData（配置资产实例，ReplicatedUsing 同步）和 AbilitySystemComponent。一堆 Handle.../Setup... 入口内部都统一调 CheckDefaultInitialization() 推进进度。

## 用户偏好（持续记录）
- 喜欢"类比 + 表格 + 口诀 + 真实源码定位"的讲解方式。
- 对编号准确性敏感，务必先核对目录。
- 明确要新建 md 时就直接新开一篇，不要问"补进旧篇还是新篇"。
- 用户是 UE5/C++ 初学者，偏好"不讲代码，只讲概念/意图/类比"的教学风格。
